--- a/too_lora_grid.py
+++ b/too_lora_grid.py
@@ -1,6 +1,7 @@
 import os
 import json
 import hashlib
+import re
 import numpy as np
 from PIL import Image, ImageDraw, ImageFont
 import folder_paths
@@ -22,7 +23,8 @@ Sources de LoRA (cumulatives) :
 Format widget :
 chemin/vers/lora.safetensors
 chemin/vers/lora.safetensors:1.0
-Les lignes vides ou commencant par # sont ignorees.
+chemin/vers/lora.safetensors:0.6:1.0
+chemin/vers/lora.safetensors:"label"  -> sm=0, sc=0, label personnalise
 """
 
@@ -57,6 +59,7 @@ class LoraGrid:
                 "loras": ("STRING", {
                     "multiline": True,
-                    "default":   "# path:str_model\n# ex: my_lora.safetensors:1.0"
+                    "default":   "# path:str_model:str_clip\n# ex: my_lora.safetensors:1.0:1.0\n# ex: my_lora.safetensors:\"no lora\""
                 }),
                 "seed":          ("INT",   {"default": 0,    "min": 0,   "max": 0xffffffffffffffff}),
@@ -74,6 +77,7 @@ class LoraGrid:
                 "label_color":   ("STRING", {"default": "#ffffff"}),
                 "bg_color":      ("STRING", {"default": "#111111"}),
+                "footer":        ("STRING", {"default": ""}),
             },
             "hidden": {
@@ -87,7 +91,7 @@ class LoraGrid:
     @classmethod
     def IS_CHANGED(cls, model, clip, vae, positive, negative, latent_image,
                    loras, seed, steps, cfg, sampler_name, scheduler, denoise,
-                   grid_cols, grid_padding, add_labels, label_height,
-                   font_size, label_color, bg_color,
+                   grid_cols, grid_padding, add_labels, label_height,
+                   font_size, label_color, bg_color, footer,
                    extra_pnginfo=None, unique_id=None):
         latent_hash = hashlib.md5(
@@ -99,7 +103,7 @@ class LoraGrid:
                loras.strip(), seed, steps, round(cfg, 4),
                sampler_name, scheduler, round(denoise, 4),
-               latent_hash, js_str)
+               latent_hash, js_str, (footer or "").strip())
         return hashlib.md5(str(key).encode()).hexdigest()
 
@@ -136,11 +140,22 @@ class LoraGrid:
         entries = []
         for e in raw:
             path_full = str(e.get("path", "")).strip()
             if not path_full:
                 continue
-            try:    sm = float(e.get("strength_model", 1.0))
-            except: sm = 1.0
-            entries.append({"path": path_full, "strength_model": sm})
+
+            m = re.search(r':"([^"]*)"$', path_full)
+            display_label = None
+            is_null = False
+            if m:
+                display_label = m.group(1)
+                is_null = True
+                path_full = path_full[:m.start()].strip()
+
+            if is_null:
+                sm, sc = 0.0, 0.0
+            else:
+                try:    sm = float(e.get("strength_model", 1.0))
+                except: sm = 1.0
+                try:    sc = float(e.get("strength_clip", 1.0))
+                except: sc = 1.0
+
+            entries.append({"path": path_full, "strength_model": sm,
+                            "strength_clip": sc,
+                            "display_label": display_label, "is_null": is_null})
         print(f"[LoraGrid] JS entries : {len(entries)}")
         return entries
 
@@ -151,13 +166,28 @@ class LoraGrid:
     @staticmethod
     def _parse_loras_text(text):
         entries = []
         for raw_line in text.splitlines():
             line = raw_line.strip()
             if not line or line.startswith("#"):
                 continue
-            parts = line.split(":", 2)
-            path  = parts[0].strip()
-            try:    sm = float(parts[1]) if len(parts) > 1 and parts[1].strip() else 1.0
-            except: sm = 1.0
-            if path:
-                entries.append({"path": path, "strength_model": sm})
+
+            m = re.search(r':"([^"]*)"$', line)
+            display_label = None
+            is_null = False
+            if m:
+                display_label = m.group(1)
+                is_null = True
+                line = line[:m.start()]
+
+            parts = line.split(":")
+            path = parts[0].strip()
+            if not path:
+                continue
+
+            if is_null:
+                sm, sc = 0.0, 0.0
+            else:
+                try:    sm = float(parts[1]) if len(parts) > 1 and parts[1].strip() else 1.0
+                except: sm = 1.0
+                try:    sc = float(parts[2]) if len(parts) > 2 and parts[2].strip() else 1.0
+                except: sc = 1.0
+
+            entries.append({"path": path, "strength_model": sm, "strength_clip": sc,
+                            "display_label": display_label, "is_null": is_null})
         return entries
 
@@ -180,7 +210,7 @@ class LoraGrid:
     @staticmethod
     def _cache_key(unique_id, model, clip, vae, positive, negative, loras, seed, steps, cfg,
-                   sampler_name, scheduler, denoise, latent_image, js_str):
+                   sampler_name, scheduler, denoise, latent_image, js_str, footer=""):
         latent_hash = hashlib.md5(
             latent_image["samples"].cpu().numpy().tobytes()
         ).hexdigest()[:8]
@@ -190,7 +220,7 @@ class LoraGrid:
                loras.strip(), seed, steps, round(cfg, 4),
                sampler_name, scheduler, round(denoise, 4),
-               latent_hash, js_str)
+               latent_hash, js_str, (footer or "").strip())
         return hashlib.md5(str(raw).encode()).hexdigest()
 
@@ -237,9 +267,14 @@ class LoraGrid:
     def _assemble_grid(self, tensors, labels, cols, padding,
-                       add_labels, label_height, font_size, fg_hex, bg_hex):
+                       add_labels, label_height, font_size, fg_hex, bg_hex, footer=""):
         pil_images = [self._tensor_to_pil(t) for t in tensors]
         n    = len(pil_images)
         cols = max(1, min(cols, n))
         rows = (n + cols - 1) // cols
 
         cell_w, cell_h = pil_images[0].size
         lh      = label_height if add_labels else 0
-        total_w = cols * cell_w + (cols + 1) * padding
-        total_h = rows * (cell_h + lh) + (rows + 1) * padding
+
+        footer = (footer or "").strip()
+        footer_h = (label_height + padding) if footer else 0
+
+        total_w = cols * cell_w + (cols + 1) * padding
+        total_h = rows * (cell_h + lh) + (rows + 1) * padding + footer_h
 
         bg_rgb    = self._hex_to_rgb(bg_hex)
         label_rgb = self._hex_to_rgb(fg_hex)
@@ -249,7 +284,7 @@ class LoraGrid:
         font = None
-        if add_labels:
+        if add_labels or footer:
             for name in ("arial.ttf", "DejaVuSans.ttf", "LiberationSans-Regular.ttf"):
                 try: font = ImageFont.truetype(name, font_size); break
                 except: pass
@@ -272,6 +307,18 @@ class LoraGrid:
                           lbl, fill=label_rgb, font=font)
 
+        if footer:
+            fy = total_h - footer_h + footer_h // 2
+            try:
+                bb = draw.textbbox((0, 0), footer, font=font)
+                tw = bb[2] - bb[0]
+            except Exception:
+                tw = len(footer) * font_size // 2
+            draw.text(((total_w - tw) // 2, fy - font_size // 2),
+                      footer, fill=label_rgb, font=font)
+
         arr = np.array(grid).astype(np.float32) / 255.0
         return torch.from_numpy(arr).unsqueeze(0)
 
@@ -281,8 +328,8 @@ class LoraGrid:
     def generate_grid(self, model, clip, vae, positive, negative, latent_image,
                       loras, seed, steps, cfg, sampler_name, scheduler, denoise,
                       grid_cols, grid_padding, add_labels, label_height,
-                      font_size, label_color, bg_color,
+                      font_size, label_color, bg_color, footer,
                       extra_pnginfo=None, unique_id=None):
 
@@ -301,7 +348,7 @@ class LoraGrid:
         js_str    = self._get_js_entries_raw(extra_pnginfo, unique_id)
         cache_key = self._cache_key(unique_id, model, clip, vae, positive, negative, loras,
                                     seed, steps, cfg, sampler_name, scheduler, denoise,
-                                    latent_image, js_str)
+                                    latent_image, js_str, footer)
 
@@ -315,27 +362,47 @@ class LoraGrid:
             for i, entry in enumerate(lora_entries):
                 path_raw = entry["path"]
                 sm       = entry["strength_model"]
-
-                lora_full = self._find_lora(path_raw)
-                if not lora_full:
-                    print(f"[LoraGrid] #{i} introuvable : {path_raw!r}")
-                    continue
-
-                print(f"[LoraGrid] #{i} {os.path.basename(lora_full)} sm={sm}")
-
-                try:
-                    weights = comfy.utils.load_torch_file(lora_full, safe_load=True)
-                    model_lora, _ = comfy.sd.load_lora_for_models(
-                        model, clip, weights, sm, 1.0)
-                except Exception as e:
-                    print(f"[LoraGrid] erreur chargement #{i} : {e}")
-                    continue
+                sc       = entry.get("strength_clip", 1.0)
+                is_null  = entry.get("is_null", False)
+                dlabel   = entry.get("display_label")
+
+                lbl = dlabel if dlabel is not None \
+                      else os.path.splitext(os.path.basename(path_raw))[0]
+
+                if is_null:
+                    print(f"[LoraGrid] #{i} null slot → '{lbl}'")
+                    try:
+                        latent_out = common_ksampler(
+                            model, seed, steps, cfg,
+                            sampler_name, scheduler,
+                            positive, negative, latent_image,
+                            denoise=denoise)[0]
+                    except Exception as e:
+                        import traceback
+                        print(f"[LoraGrid] erreur sampling null #{i} : {e}")
+                        traceback.print_exc()
+                        continue
+                    try:
+                        decoded = self._vae_decode(vae, latent_out)
+                    except Exception as e:
+                        print(f"[LoraGrid] erreur VAE decode null #{i} : {e}")
+                        continue
+                    cached.append((decoded[0].clone(), lbl))
+                    print(f"[LoraGrid] OK #{len(cached)} (null) {decoded.shape}")
+                    continue
+
+                lora_full = self._find_lora(path_raw)
+                if not lora_full:
+                    print(f"[LoraGrid] #{i} introuvable : {path_raw!r}")
+                    continue
+
+                print(f"[LoraGrid] #{i} {os.path.basename(lora_full)} sm={sm} sc={sc}")
+
+                try:
+                    weights = comfy.utils.load_torch_file(lora_full, safe_load=True)
+                    model_lora, _ = comfy.sd.load_lora_for_models(
+                        model, clip, weights, sm, sc)
+                except Exception as e:
+                    print(f"[LoraGrid] erreur chargement #{i} : {e}")
+                    continue
 
                 try:
@@ -357,12 +404,10 @@ class LoraGrid:
                     print(f"[LoraGrid] erreur VAE decode #{i} : {e}")
                     traceback.print_exc()
                     continue
 
-                lbl = os.path.splitext(os.path.basename(path_raw))[0]
                 cached.append((decoded[0].clone(), lbl))
                 print(f"[LoraGrid] OK #{len(cached)} {decoded.shape}")
 
@@ -381,7 +426,7 @@ class LoraGrid:
         grid = self._assemble_grid(
             image_tensors, labels, grid_cols, grid_padding,
-            add_labels, label_height, font_size, label_color, bg_color)
+            add_labels, label_height, font_size, label_color, bg_color, footer)
 
 
--- a/lora_grid_node.js
+++ b/lora_grid_node.js
@@ -19,6 +19,7 @@ app.registerExtension({
     nodeType.prototype._syncEntriesFromWidgets = function () {
       for (let i = 0; i < this.lora_entries.length; i++) {
         const path = this._widgetValue(`path_${i}`);
         const sm   = this._widgetValue(`str_model_${i}`);
+        const sc   = this._widgetValue(`str_clip_${i}`);
         if (path !== undefined) this.lora_entries[i].path           = path;
         if (sm   !== undefined) this.lora_entries[i].strength_model = parseFloat(sm) || sm;
+        if (sc   !== undefined) this.lora_entries[i].strength_clip  = parseFloat(sc) || sc;
       }
     };
 
@@ -132,6 +133,16 @@ app.registerExtension({
         const smW = add({
           name: `str_model_${i}`, type: "string", value: String(entry.strength_model ?? 1.0),
           computeSize: (w) => [w, 20],
           callback: (v) => {
             const n = Math.max(-4, Math.min(4, parseFloat(v)));
             smW.value = isNaN(n) ? smW.value : String(n);
             if (!isNaN(n)) self.lora_entries[i].strength_model = n;
           }
         });
 
+        const scW = add({
+          name: `str_clip_${i}`, type: "string", value: String(entry.strength_clip ?? 1.0),
+          computeSize: (w) => [w, 20],
+          callback: (v) => {
+            const n = Math.max(-4, Math.min(4, parseFloat(v)));
+            scW.value = isNaN(n) ? scW.value : String(n);
+            if (!isNaN(n)) self.lora_entries[i].strength_clip = n;
+          }
+        });
+
         add({
           name: `remove_${i}`, type: "button", serialize: false,
@@ -171,6 +182,7 @@ app.registerExtension({
         self.lora_entries.push({ path: "", strength_model: 1.0, strength_clip: 1.0 });
